<?php

/** @var \Illuminate\Database\Eloquent\Factory $factory */

use App\ScheduleNote;
use Faker\Generator as Faker;

$factory->define(ScheduleNote::class, function (Faker $faker) {
    return [
        //
    ];
});
